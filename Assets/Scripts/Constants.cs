public enum Platform
{
    Editor,
    Mobile
}

public enum ProjectileType
{
    Bullet = 0,
    Bomb = 1
}

public enum SurfaceType
{
    ColoredCube = 0
}

public enum FPSMode
{
    LowPerformance = 10,
    Default = 60
}